[[source]]
verify_ssl = true
name = "pypi"
url = "https://pypi.python.org/simple"

[packages]
typer = ">=0.4"
Flask-WTF= ">=0.15.1"
cryptography = ">=3.3.2"
flask = ">=2.0.2"
markdown = ">=2.6.11"
pygments = ">=2.2.0"

[dev-packages]
pytest = ">=5.3.0"
pytest-cov = ">=2.7.1"
pytest-mock = ">=1.10.4"
setuptools = "*"

[requires]
python_version = "3.11"

[scripts]
test = "pytest --cov=blask --cov-report=xml"
linter =  "pylint blask --exit-zero"
